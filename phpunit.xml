<?xml version="1.0" encoding="UTF-8"?>
<phpunit xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:noNamespaceSchemaLocation="vendor/phpunit/phpunit/phpunit.xsd"
         bootstrap="vendor/autoload.php"
         colors="true"
         displayDetailsOnTestsThatTriggerWarnings="true"
         displayDetailsOnTestsThatTriggerErrors="true"
         failOnWarning="false"
>
    <testsuites>
        <!--
            Unit    - pure domain logic: money, pricing, taxes, availability
                      arithmetic, automation conditions. No HTTP, minimal DB.
            Feature - services and workflows against the real database.
            Api     - the HTTP surface, including authorization and tenancy.
            Security- tenant isolation, privilege escalation, webhook auth.
        -->
        <testsuite name="Unit">
            <directory>tests/Unit</directory>
        </testsuite>
        <testsuite name="Feature">
            <directory>tests/Feature</directory>
        </testsuite>
        <testsuite name="Api">
            <directory>tests/Api</directory>
        </testsuite>
        <testsuite name="Security">
            <directory>tests/Security</directory>
        </testsuite>
    </testsuites>
    <source>
        <include>
            <directory>app</directory>
        </include>
    </source>
    <php>
        <env name="APP_ENV" value="testing"/>
        <env name="APP_MAINTENANCE_DRIVER" value="file"/>
        <env name="BCRYPT_ROUNDS" value="4"/>
        <env name="BROADCAST_CONNECTION" value="null"/>
        <env name="CACHE_STORE" value="array"/>

        <!--
            The suite runs against PostgreSQL rather than SQLite. The schema
            relies on jsonb, partial unique indexes, check constraints and
            SELECT ... FOR UPDATE, so testing on a different engine would not
            exercise the guarantees the product depends on.
        -->
        <env name="DB_CONNECTION" value="pgsql"/>
        <env name="DB_DATABASE" value="bnb_testing"/>

        <env name="MAIL_MAILER" value="array"/>
        <env name="QUEUE_CONNECTION" value="sync"/>
        <env name="SESSION_DRIVER" value="array"/>
        <env name="TELESCOPE_ENABLED" value="false"/>
        <env name="PAYMENTS_DEFAULT_PROVIDER" value="mock"/>
        <env name="AI_DEFAULT_PROVIDER" value="echo"/>
        <env name="LOCKS_DEFAULT_PROVIDER" value="mock"/>
    </php>
</phpunit>
